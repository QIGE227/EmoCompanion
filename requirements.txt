# ============================================================
# 情感陪伴数字人APP — 依赖清单
# 开发环境: Python 3.10+ / PyQt5
# 目标平台: Android (via Buildozer / python-for-android)
# ============================================================

# ----- 核心框架 -----
PyQt5>=5.15.9
PyQt5-sip>=12.13.0
numpy>=1.24.0
opencv-python>=4.8.0

# ----- 人脸 & 情绪 -----
mediapipe>=0.10.7

# ----- 大语言模型 (Qwen2 7B) -----
transformers>=4.40.0
torch>=2.1.0
accelerate>=0.27.0
bitsandbytes>=0.42.0          # 4‑bit 量化（移动端必选）
sentencepiece>=0.1.99
# 可选：llama-cpp-python      # GGUF 推理（更适合 APK 场景）

# ----- 语音识别 (Whisper) -----
faster-whisper>=1.0.3
# 可选：openai-whisper        # 原始实现

# ----- 语音合成 (Coqui TTS) -----
TTS>=0.22.0
# 或 Piper TTS (轻量替代，推荐移动端)
piper-tts>=1.4.0

# ----- 音视频处理 -----
sounddevice>=0.4.6
soundfile>=0.12.1
pyaudio>=0.2.13
ffmpeg-python>=0.2.0
librosa>=0.10.1

# ----- 图形渲染 -----
PyOpenGL>=3.1.7

# ----- 工具类 -----
PyYAML>=6.0
requests>=2.31.0
pillow>=10.0.0

# ----- APK 打包 (仅开发机) -----
# buildozer>=1.5.0
# cython>=0.29.37
